from setuptools import setup, find_packages

setup(
    name='mixed-sample-sgd',
    version='0.1.0',
    description='PyTorch implementation of Mixed-Sample SGD for Transfer Learning',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        'torch>=1.8.0',
    ],
)